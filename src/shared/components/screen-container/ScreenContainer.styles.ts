import {StyleSheet} from 'react-native';

const styleSheet = () =>
  StyleSheet.create({
    safeArea: {
      flex: 1,
    },
    scrollView: {
      flexGrow: 1,
    },
    container: {
      flex: 1,
    },
  });
export default styleSheet;
